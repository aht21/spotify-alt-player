import styles from "./tracksList.module.css";

interface Props {
  items: {
    id: string;
    name: string;
  }[];
}

const TracksList = () => {
  return <div></div>;
};

export default TracksList;
